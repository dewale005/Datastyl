// Exporting controller modules to be used in other parts of the application

// Export the authentication controller from 'auth.controller' file
module.exports.authController = require("./auth.controller");

// Export the user controller from 'user.controller' file
module.exports.userController = require("./user.controller");
